# 07 - Login com Sessão

Conceitos: `session_start()`, `password_hash()`, `password_verify()`, `header('Location: ...')`, área protegida.

Demo: `admin` / `admin123` (hash gerado com `PASSWORD_DEFAULT`).
